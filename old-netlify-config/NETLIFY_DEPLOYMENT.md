# Руководство по Деплою на Netlify

Это руководство поможет вам развернуть приложение расписания РГСУ на Netlify.

## Почему Netlify?

- ✅ **100 GB bandwidth** бесплатно (vs 10 GB на Vercel)
- ✅ Полная поддержка Next.js 16
- ✅ Работает на Windows без проблем
- ✅ Простая настройка через Git
- ✅ Автоматические деплои

## Предварительные Требования

- ✅ Аккаунт Netlify (бесплатный план подходит)
- ✅ Git репозиторий на GitHub или GitLab
- ✅ Все изменения закоммичены и запушены

## Шаг 1: Подготовка Проекта

### 1.1 Проверка Зависимостей

Убедитесь, что у вас установлены правильные версии:

```bash
npm install
```

### 1.2 Проверка Локальной Сборки

```bash
npm run build
```

## Шаг 2: Создание Проекта на Netlify

### 2.1 Вход в Netlify

1. Перейдите на [app.netlify.com](https://app.netlify.com)
2. Войдите или создайте бесплатный аккаунт

### 2.2 Создание Нового Сайта

1. Нажмите **Add new site** → **Import an existing project**
2. Выберите **Deploy with GitHub** (или GitLab)
3. Авторизуйте Netlify для доступа к репозиториям
4. Выберите репозиторий `shedule`

### 2.3 Настройка Сборки

Netlify автоматически определит Next.js. Проверьте настройки:

- **Branch to deploy**: `main` (или ваша основная ветка)
- **Build command**: `npm run build`
- **Publish directory**: `.next`

Нажмите **Deploy site**

## Шаг 3: Настройка Переменных Окружения

### 3.1 Добавление Environment Variables

1. В настройках сайта перейдите в **Site configuration** → **Environment variables**
2. Нажмите **Add a variable**
3. Добавьте все переменные из `.env`:

```
# URL сайта (обновите после первого деплоя)
NEXT_PUBLIC_SITE_URL=https://ваш-сайт.netlify.app

# Upstash Redis
UPSTASH_REDIS_REST_URL=your_upstash_url
UPSTASH_REDIS_REST_TOKEN=your_upstash_token

# Или KV (если используете)
KV_REST_API_URL=your_kv_url
KV_REST_API_TOKEN=your_kv_token

# Google Service Account
GOOGLE_CLIENT_EMAIL=your_service_account_email
GOOGLE_PRIVATE_KEY=your_private_key

# VAPID ключи (для push-уведомлений)
VAPID_PUBLIC_KEY=your_vapid_public_key
VAPID_PRIVATE_KEY=your_vapid_private_key
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your_vapid_public_key

# Gemini AI
GEMINI_API_KEY=your_gemini_api_key
```

> **Важно**: Для многострочных значений (например, `GOOGLE_PRIVATE_KEY`) используйте текстовое поле и вставьте значение с переносами строк.

### 3.2 Обновление URL

После первого деплоя Netlify даст вам URL вида `https://ваш-сайт.netlify.app`. Обновите переменную:

```
NEXT_PUBLIC_SITE_URL=https://ваш-сайт.netlify.app
```

Затем **Redeploy** сайт для применения изменений.

## Шаг 4: Проверка Деплоя

После успешной сборки (обычно 2-5 минут):

1. Перейдите по URL вашего сайта
2. Проверьте основные функции:
   - ✅ Загрузка расписания
   - ✅ Выбор даты
   - ✅ Экспорт в PDF
   - ✅ Переключение темы
   - ✅ PWA установка

## Шаг 5: Настройка Cron Job

Netlify не поддерживает cron jobs напрямую. Используйте один из вариантов:

### Вариант 1: Netlify Scheduled Functions (Рекомендуется)

1. Создайте файл `netlify/functions/scheduled-push-check.mjs`:

```javascript
import { schedule } from '@netlify/functions';

export const handler = schedule('0 8 * * *', async () => {
  const response = await fetch('https://ваш-сайт.netlify.app/api/push/check');
  return {
    statusCode: 200,
  };
});
```

2. Установите зависимость:
```bash
npm install --save-dev @netlify/functions
```

3. Закоммитьте и запушьте изменения

### Вариант 2: Внешний Сервис (cron-job.org)

1. Зарегистрируйтесь на [cron-job.org](https://cron-job.org)
2. Создайте новый cron job
3. URL: `https://ваш-сайт.netlify.app/api/push/check`
4. Расписание: `0 8 * * *` (каждый день в 8:00)

## Шаг 6: Настройка Кастомного Домена (Опционально)

1. В настройках сайта перейдите в **Domain management**
2. Нажмите **Add custom domain**
3. Введите ваш домен
4. Следуйте инструкциям по настройке DNS

## Автоматические Деплои

Netlify автоматически деплоит изменения при каждом push:

- **Production**: деплой из `main` ветки
- **Deploy Previews**: деплой из pull requests

## Troubleshooting

### Ошибка сборки: "Module not found"
```bash
npm install
npm run build
```

### Переменные окружения не работают
- Проверьте, что все переменные добавлены в Netlify Dashboard
- Сделайте **Redeploy** после добавления переменных

### API Routes не работают
- Netlify автоматически обрабатывает Next.js API routes как serverless functions
- Проверьте логи функций в **Functions** разделе

## Мониторинг

- **Build logs**: доступны в разделе **Deploys**
- **Function logs**: доступны в разделе **Functions**
- **Analytics**: доступны в разделе **Analytics** (требует настройки)

## Лимиты Бесплатного Плана

- ✅ **Bandwidth**: 100 GB/месяц
- ✅ **Build minutes**: 300 минут/месяц
- ✅ **Serverless functions**: 125,000 запросов/месяц
- ✅ **Function runtime**: 10 секунд на запрос

## Полезные Ссылки

- [Netlify Docs](https://docs.netlify.com/)
- [Next.js on Netlify](https://docs.netlify.com/frameworks/next-js/)
- [Netlify Functions](https://docs.netlify.com/functions/overview/)

---

## Следующие Шаги

После успешного деплоя:

1. ✅ Обновите `NEXT_PUBLIC_SITE_URL` на актуальный URL
2. ✅ Настройте cron job для `/api/push/check`
3. ✅ (Опционально) Подключите кастомный домен
4. ✅ Удалите проект с Cloudflare Pages (если создавали)
5. ✅ Удалите проект с Vercel (если больше не нужен)

**Поздравляем! Ваше приложение теперь работает на Netlify! 🎉**
